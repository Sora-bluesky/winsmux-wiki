---
import src from '../../../raw/japanese.md?raw';
import { buildDoc } from '../../../lib/doc';
import DocPage from '../../../components/DocPage.astro';

const doc = buildDoc('japanese', src);
---

<DocPage
  title={doc.title}
  description={doc.description}
  rawPath={doc.rawPath}
  sources={doc.sources}
  hermesVersion={doc.hermesVersion}
  confidence={doc.confidence}
  tocRail={doc.tocRail}
  html={doc.html}
/>
