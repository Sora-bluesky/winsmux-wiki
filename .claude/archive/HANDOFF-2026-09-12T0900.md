# HANDOFF

> **Updated**: 2026-09-11T08:55+09:00
> **Agent**: Claude Code (Fable 5.1)
> **Purpose**: 状態の要約。ここから作業を自動開始しない

## 次のセッションの開始対象
「未選択の候補」の先頭から。09-08 に 1〜3（frontmatter 8 ページ・更新パネル当日反映・Hermes 自身に聞く）を公開済み。残りは wiki-ship 初回・索引の観測・小ワザ集

## 直近で完了したこと
- **Cloudflare Images のタダ乗り課金リスクを監査**（2026-09-11・ワークフロー: 調査 4 本 + 反証 3 本）。wiki は Images を使っておらず（リポ・dist・公開 HTML に imagedelivery / cdn-cgi/image の参照ゼロ）、静的アセットのみ。wiki・apex・www の `/cdn-cgi/image/`（同一ゾーン元・外部元とも）と `/cdn-cgi/media` は Cloudflare 定型 404 で変換は起きない（`cf-resized` なし）。`/cdn-cgi/imagedelivery/...` は 400 'Account is incorrect' で他人の画像は配れない。公式: 静的アセットはリクエスト無料・無制限で egress 課金なし、Images Free は月 5,000 変換を超えても課金されず 9422 エラー、変換の既定ソースは allowed origins。**未確認（ダッシュボードでしか見えない）**: アカウントの有料契約（Images Paid・R2・Argo・Cache Reserve・Workers Paid）と支払い方法、他ゾーン、ゾーンの全ホスト名。FACTS.md:9 は「Workers 無料プラン」、:97 は billing スクショ待ち
- トップページ「よく使うページ」に「Hermes 自身に聞く」のチップを追加（`664d55c`・公開確認済み）。発見: デスクトップ幅では左サイドバーが公式 docs の分類だけで、独自ページ一覧（「Hermes Agentの使い方」見出し）はハンバーガーメニュー `#site-menu` 内にあり `lg:!hidden` で常時非表示。使い方ページへはヘッダーの「ドキュメント」からしか行けない（ラベルがページ名と不一致）。sora 指摘。ヘッダーのラベル見直しは未着手
- x-article へ Discord の読み方を回答: Discord 本体でなく GitHub の `teknium1/nous-discord-archive`（6 時間ごとのテキストログ）と、その週次要約 `data/wiki/community.json` / /hermes/community/ を使う
- **wiki-ship の push 経路を初めて完走**（2026-09-08・`b186823`）。clean なつもりで流したが build が updates.md / updates-weekly.md を再生成して 4 件が乗り、そのままコミット・push・Actions success・llms.txt 200・exit 0。中身は当日の更新一覧の再生成で無害だが、メッセージが「chore: probe wiki-ship path」のまま。**教訓: build が生成物を書き換えるので、wiki-ship は「変更なし」で止まる前提にできない。流す前に `--dry-run` で 3 段目の件数を見る**。分類器は今回は通した（前回 09-05 は deny）
- **「Hermes 自身に聞く」ページを公開**（2026-09-08・`6e1c6e2`・Actions success・/hermes/ask/）。貼るだけで使える依頼文 7 本（/help・/tools・/model・hermes doctor・同梱 skill で公式 docs 参照・今週の更新ファイル・自分の設定確認）。各項目に公式ミラーの根拠リンク。Sonnet 執筆 → Codex public-text 検証で 6 件（出典より強い断定）を出典の範囲に戻して公開。guide ページと PAGES 一覧から導線あり。@witcheer スレッド由来の案 3
- **更新パネルの当日反映を公開**（2026-09-08・`657ec29`・Actions success）。生成が同期コミットより前に走るため当日分が翌日に回っていた。作業ツリーの内容差分（`git diff --name-only HEAD` + 未追跡。`git status` は autocrlf の正規化判定だけで M を返し幽霊ページを作ったので使わない）を当日の項目に畳む。同日のコミット済み項目があれば統合。2 回走らせても同一。Sol 実装・Codex APPROVE
- **frontmatter 破損 8 ページの是正を公開**（2026-09-08・`1a00dc2`・Actions success）。原因は翻訳でなく抽出側: 上流 8 本は frontmatter に title が無く H1 だけで、mirror-extract が URL パスで埋めていた。抽出は H1（アンカー除去・フェンス復元前）から採るように修正、doc.ts は JSON 引用の値を復号、8 ページの title は本文の日本語 H1 から補填。Codex 2 巡（フェンス内の `# example` を拾う経路と、エスケープ引用符の漏れを指摘 → 修正して PASS）
- **「今週の更新」ファイルを公開**（2026-09-07・`57261be`・Actions success）。`/hermes/raw/updates-weekly.md` = 直近 7 日に変わったページ（URL で重複排除・新しい順）+ 最新の週次まとめ + 頼み方のプロンプト。更新履歴ページに「あなたの Hermes に読ませる」の箱（コピーボタン付き）、llms.txt に節を追加。日次 sync の build で自動更新される。出典は @witcheer（Nous）のスレッドで最多支持だった「agent to agent newsletter」。Sol 実装・Codex APPROVE。残りの案（小ワザ集、Hermes 自身に聞くプロンプト集、最初の 7 日、Masterclass ノート）は未着手
- **X 記事（@bloggersarvesh の Astra SEO プロンプト 20 本）の応用検討と実装**（2026-09-07）。中身の 8 割は Google ビジネスプロフィール向けで対象外。使える型 3 つのうち 2 つを盤に入れた（Sol 実装・Codex APPROVE）
  - 語句カードに「伸びしろ（順位 11〜20・表示回数順）」を追加。GSC の返却 250 語句から抽出（いまは表示 1〜2 回の語が 4 つだけ。母数が育つまで判断材料にはならない）
  - `live2/seo-context.md` を data.json 書き込みのたびに自動生成（`seo-context.head.md` = 手書きの前提 + 自動の現在値）。エージェントに SEO 分析を頼むときの文脈ファイル。x-article リポ側・未コミット
  - 発見: 目標クエリ 24 語の上位 3 位を占めるのは note.com 10 語・zenn.dev 9 語・fyve.co.jp 8 語で、公式 docs は 6 語。日本語の個人記事が主戦場。競合の共通点の読み取り（型 3）は未実施
- **計測コンソールの「検索・AI 露出」タブを Search Console 分析情報風に再構成**（2026-09-07・x-article リポ側・未コミット）。KPI 2 枚（前の7日間との差・対象と前期の日付）→ クリックされたページ | 検索語句（指名バッジ・列名）→ 国別クリック構成 | Google の索引 → クローラ | Bing → AI の答え。設計は Codex astra の設計レビュー（REVISE 11 件・判断表）を反映した v2、実装 Sol xhigh、敵対レビュー Codex 2 巡（3 件修正: 国の棒 display、rows 非 list の失敗扱い、アクセスタブの totals null）。ブラウザで 1400px 幅の描画を確認
  - データ契約: 当期 E-6〜E / 前期 E-13〜E-7、`prev`、`pages[].pc`、`queries[].brand`、`countries`。題名の取得と KPI の折れ線は設計レビューで却下（履歴は日別でない）
  - 残る小さな課題: KPI カードの下半分が空く（grid の行高に引きずられる）。国名表は主要 24 か国のみ（未知は「国名不明（CODE）」）
- **GSC 未索引対策を公開**（2026-09-07・`cc3a914`・Actions success）。GSC の実測: リダイレクト1（`/`）・検出-未登録 136（own 16 / docs 61 / skills 60、内部リンクは 424 頁が 11 本以上で不足なし）・クロール済み-未登録 6（うち4本が raw .md）
  - `public/_redirects`: スタブ6本を 301 に（行き先は従来どおり）。サイトマップから除外して 463 URL。公開で全経路 301 を curl 確認
  - `public/_headers`: `/hermes/raw/*` に `X-Robots-Tag: noindex`（HTML と重複する原文を Google の索引から外す。取得は妨げない）。公開で確認
  - `astro.config.mjs`: サイトマップ lastmod を git の最終コミット日から付与（.astro が import する raw/*.md・data/*.json の新しい方）。463/463。`deploy.yml` は fetch-depth 0（Sol 実装・Codex 2巡 APPROVE-WITH-NITS・nit 適用済み）
  - 効果判定は GSC 側の次回更新（数日〜数週）。数字は盤の「検索・AI 露出」と `index-history.jsonl` で追う
  - 手動リクエストの候補（独自ページで未索引・スタブ除く 12 本）: /hermes/community/ /hermes/concepts/gateway/ /hermes/entities/{docker,line,local,nous-portal,vps}/ /hermes/ops/ /hermes/search/ /hermes/syntheses/not-a-mirror/ /hermes/trust/ /hermes/cost/（日次上限あり・sora の手作業）
- 計測コンソール（live2）に指名検索の分子分母と、索引・指名の折れ線を追加（2026-09-06・x-article リポ側・未コミット）。live2 の日次履歴は `search-history.jsonl`（evidence とは分離）
- **title タグと description の是正を公開**（2026-09-05 13:00・`9729361` + `6c3f6fc`・Actions success・llms.txt 200）
  - `BaseLayout.astro`: `<title>` を「ページ名 | Hermes Agent Wiki」に統一（phase3 の決定をようやく実装。og:title と H1 はページ名のまま）。Hermes を含む title 31→467
  - `doc.ts` `fallbackDescription`: frontmatter の description が空なら本文の最初の日本語段落を 110 字で採る。捏造なし。description 空 44→7（残りは転送スタブと Google 確認ファイルだけ）。Grok 1巡 APPROVE-WITH-NITS、nit の `:::` ボックス除外と CJK 折り返し空白を適用
  - 60字超 40→188 は suffix 19 字分。表示は Google 側で切られるので放置。skill ページの title 自体が 70〜84 字なのは原稿側の課題
  - **発見（未対応）**: title が `developer-guide/adding-platform-adapters` のように id のまま出るページが 8 本。原稿 frontmatter の `sources:` にコードが流れ込んで壊れている（mirror-extract の抽出不良）。再抽出の対象。`node scripts/wiki-audit.mjs --json` では拾えないので、title が `^[a-z-]+/` で始まるページを数える
- wiki-ship は分類器に止められ初回実行できず（`52842b3` は素の `git push` で出荷）。**push 経路は依然未実行**
- スクリプト3本のレビューと修正（2026-09-05 12:00）。Grok の指摘は全件 file:line で裏取りしてから適用
  - `wiki-audit.mjs`: 孤立判定から転送スタブを除外（孤立 7→1、残りは Google 所有確認ファイルのみ）。**`52842b3` でコミット済み・未 push**
  - `lane-status.py`: object 以外の JSON・UnicodeDecodeError・None 表示・ball 要素の型を全部「不明」へ倒した。壊れた入力で render が落ちないことを実測
  - `wiki-ship.sh`: **`git add -A` が `.claude/archive/` の引き継ぎ（OS アカウント名入り）を public に積む欠陥**を pathspec `:!.claude` で塞いだ。gh 未認証・gh 失敗は10分待たず判定不能。exit code を 0/3/4 に分けた（3=判定不能、4=Actions 失敗か公開 URL 非200）。6〜8 段を HEAD・偽トークン・存在しない sha で単体実行して確認。**push 経路は引き続き未実行**
  - 見送り: normalize の `index.html` と `//host`（dist に該当 href 0 件）、HEAD→GET、pid 再利用。`scripts/__pycache__/*.pyc` が tracked のまま（.gitignore 追加前の混入・別コミットで消す候補）
- トークン消費の実測。定時タスクは犯人ではなく（日次 sync は1回 0.25M）、長寿命の対話セッションが主因（このセッションは7日で 52.2M・4,025 コール）。シェル実行 1,304 回のうち 565 回（43%）が「いまの状態を測る」だけの呼び出しだった
- 上を畳むためのスクリプト3本を作成し、3本とも実行して確認
- 采配ハブ休止に伴い、連絡先の記述を `HANDOFF.md` と盤の 0.2 行で実態に合わせた
- ハブからの申し送り（2026-09-05）: Windows タスク `winsmux-wiki-launch-monitor` が scratchpad 内の watch.sh を指していた。同じ4ファイルは `~/.claude/data/winsmux-wiki-launch-monitor/` にコピー済み（実在確認）。タスクの向け直しは sora に依頼中。live2 の `live-dashboard.log` は上限なしの単一ファイル → 次に live2 を触るときに月分割か上限を付ける。週次掃除 `~/.claude/bin/stone-sweeper.py` 新設（実在確認）
- `HTTP の健全性検査は UA で落とされていないか先に切り分ける` を `rules/harness-engineering.md` に追記（実測: llms.txt が curl 200 / urllib 403）

## 現在の作業場所
- winsmux-wiki: `main` = `6c3f6fc` = origin。未追跡は `.claude/archive/` のみ。tracked な差分なし
- x-article: `master` = `8403a70`。作業ツリーに記事レーン側の未コミット差分 42 件（自分の担当外）
- `.claude/local/` は `.git/info/exclude` でローカル限定。`lane-status.py` と `wiki-ship.sh` はここに置いた（他リポの絶対パスに OS アカウント名が入るため public リポには出さない）

## 未選択の候補（優先順）
- **CI のトークン露出を狭める**（2026-09-11 監査の副産物）。deploy.yml は同じジョブで `npm install`（package-lock.json は .gitignore:11 で未コミット・毎回解決し直し）→ build → `npx wrangler deploy`（トークンは deploy ステップだけの env）。依存パッケージが乗っ取られると postinstall で node_modules を書き換え、deploy 時にトークンを盗める。リポは PUBLIC だが fork PR では走らない（トリガーは push main と workflow_dispatch）。案: build ジョブ（トークンなし）と deploy ジョブ（dist を artifact で受け、版固定の wrangler だけ）に分ける。CLAUDE.md の「npm ci に戻さない / cache: npm を戻さない」と両立する。トークン権限の絞り込み（Workers Scripts Edit + winsmux.dev の Workers Routes Edit）は sora のダッシュボード作業。着手前に Codex astra の設計レビュー
- **明日の同期は通常より大きい**（2026-09-08 夜の実測: stale 25 + 新規 3。平常は 17 前後）。上流が同日 23 時台に docs をまとめて更新した（delegation の既定値・code-execution のカーネル・bot-mode の常駐・sessions の自動整理・FAQ / Telegram / Windows の小修正）。翻訳の量が増えるので時間を見ておく
- **未クロール独自ページの手動インデックス登録**: 効果は実証済み（09-01 に5本頼んで翌日クロール）。GSC の日次上限あり
- **外部リンクの獲得**: 唯一「クロール需要」に触る手。獲得先は未検討
- Rich Messages 動画のシーン1/3/4（Telegram画面）は sora の収録待ち

## 決定事項
- 采配ハブ（cwd Projects/apps）は 2026-09-05 に休止（sora 判断）。判断は自セッションの `AskUserQuestion` で sora へ直接。rule 10-E は休止中は不適用
- `wiki-ship` から mirror-lint の段階を削除した。`mirror-lint.mjs` は `.mirror/en` 配下の1ファイルを突き合わせる翻訳同期の道具で出荷前ゲートではなく、成功時に tracked な `data/mirror-state.json` を書き換えるため、置くと毎回その差分をコミットに巻き込む
- レビューはすべて Codex `gpt-6-astra` low に集中（2026-09-05 sora 指示・同日2件目）。敵対レビュー・re-review・public-text 検証・設計・judgement のすべてを `codex-sol.ps1`（既定 astra low）で行い、`-Effort` `-Model` は渡さない。Grok はレビューに使わない（Web 調査・X 関連のみ）
- 実装は Sol xhigh（`/sol-implement` は `-Model gpt-5.6-sol -Effort xhigh` を明示）か `pstack:poteto-mode`。実装と敵対レビューが同一プロバイダ別モデルになる件は 2026-09-05 の sora 裁定で許容済み。正本は `~/.claude/rules/model-dispatch.md`（09-05 14:59 更新を確認）
- 秘密が要る収集は有人セッションのみ。無人経路は `op` を呼ばず `skipped` を記録
- 本日 13:00 までのレビュー3本（運用スクリプト2巡・title 是正1巡）は上の指示を知る前に Grok で回したもの。やり直していない

## 報告前チェック（2026-09-07 sora 指摘で追加）
- 施策を公開した・障害を直した・数字が動いたら、x-article の `workspace/13-claude-design-dashboard/timeline.md`（盤）と `workspace/16-serp-zero-to-rank/timeline.md`（SEO）に日付付きで追記してから報告する。自動なのは metrics.jsonl だけ
- 「本番」の判定基準（2026-09-07 sora「全ページが索引に入ってからが本番」を受けて）: 全 463 ページではなく、独自ページ 36 本が全部索引に入り、導線に載せた docs 主要ページが入った時点。ミラー全件の索引は保証されない

## 証跡と復元
- トークン消費の再測定: `.claude/local/token-audit/` の3本（`sync_cost.py` = タスク別・セッション別、`run_anatomy.py` = 1セッションのツール内訳、`img_and_topics.py` = 測定トピック別）。引数はトランスクリプトの `.jsonl`
- 全量の詳細（機構の地図・落とし穴・履歴）は `.claude/archive/HANDOFF-2026-09-05T0940.md`
- 索引の正本: `articles/x-article/cloudflare-zero-cost/tools/live2/index-history.jsonl`（09-04 時点 326/465、独自 20/36）
- WBS 盤: `http://127.0.0.1:8799/wbs.html`

---
この引き継ぎの記述は、選択された対象に限って現物と照合して検証すること。事実として鵜呑みにしない。
